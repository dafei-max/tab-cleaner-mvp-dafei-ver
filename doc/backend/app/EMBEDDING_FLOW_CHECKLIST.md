# Embedding 流程检查清单

## 1. OpenGraph 信息收集后什么时候请求 Embedding？

### ✅ 主要触发点

#### 1.1 在 OpenGraph 抓取时自动触发（推荐）
**位置**: `opengraph.py` → `fetch_opengraph()` → `_prefetch_embedding()`

**触发时机**:
- ✅ 当 OpenGraph 数据解析**成功后**立即调用
- ✅ 在多个成功分支中都会调用（行 188, 229, 264, 300, 308, 334）
- ✅ 异步执行，不阻塞主流程

**流程**:
```
fetch_opengraph(url)
  ↓
解析 OpenGraph 数据成功
  ↓
调用 _prefetch_embedding(result)
  ↓
1. 检查数据库是否已有 embedding
   - 有 → 直接使用，不重新生成
   - 无 → 继续
2. 生成 text_embedding（使用 embed_text）
3. 生成 image_embedding（使用 embed_image）
4. 存储到向量数据库（upsert_opengraph_item）
```

**代码位置**:
- `opengraph.py:411-515` - `_prefetch_embedding()` 函数
- `opengraph.py:69-407` - `fetch_opengraph()` 函数（多处调用 `_prefetch_embedding`）

#### 1.2 通过 API 手动请求
**位置**: `main.py` → `/api/v1/search/embedding`

**触发时机**:
- ✅ 前端调用 `/api/v1/search/embedding` API
- ✅ 当 OpenGraph 数据没有 embedding 时
- ✅ 批量处理多个 items

**流程**:
```
POST /api/v1/search/embedding
  ↓
1. 优先从数据库读取（get_opengraph_item）
   - 有 → 直接返回
   - 无 → 继续
2. 调用 process_opengraph_for_search() 生成 embedding
3. 存储到数据库（如果配置了）
4. 返回结果给前端
```

**代码位置**:
- `main.py:166-304` - `generate_embeddings()` 函数

### 📝 检查要点

- [ ] `_prefetch_embedding()` 是否在所有成功分支都被调用？
- [ ] 数据库检查逻辑是否正确（避免重复生成）？
- [ ] 错误处理是否完善（失败不影响主流程）？
- [ ] API 是否优先从数据库读取？

---

## 2. Embedding 数据都存在了哪里？

### ✅ 存储位置

#### 2.1 主要存储：阿里云 AnalyticDB PostgreSQL
**数据库**: 由 `ADBPG_DBNAME` 环境变量决定（默认 `knowledgebase`，但实际运行时可能是 `postgres`）
**Schema**: `cleantab` (由 `ADBPG_NAMESPACE` 环境变量控制，默认 `cleantab`)
**表名**: `opengraph_items`

**⚠️ 重要**：实际数据库名称以运行时环境变量为准！
- 如果 `.env` 中 `ADBPG_DBNAME=postgres`，则实际表为：`postgres.cleantab.opengraph_items`
- 如果 `.env` 中 `ADBPG_DBNAME=knowledgebase`，则实际表为：`knowledgebase.cleantab.opengraph_items`
- 可通过日志确认：`Schema 'cleantab' exists`、`Table cleantab.opengraph_items already exists`

**表结构**（实际数据库名以环境变量 `ADBPG_DBNAME` 为准）:
```sql
-- 如果 ADBPG_DBNAME=postgres，则实际为 postgres.cleantab.opengraph_items
-- 如果 ADBPG_DBNAME=knowledgebase，则实际为 knowledgebase.cleantab.opengraph_items
CREATE TABLE cleantab.opengraph_items (
    url TEXT PRIMARY KEY,                    -- 主键
    title TEXT,
    description TEXT,
    image TEXT,
    site_name TEXT,
    tab_id INTEGER,
    tab_title TEXT,
    text_embedding vector(1024),            -- 文本 embedding（1024维）
    image_embedding vector(1024),          -- 图像 embedding（1024维）
    metadata JSONB,                         -- 元数据（is_screenshot, is_doc_card等）
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);
```

**索引**:
- `idx_opengraph_url` - URL 索引
- `idx_text_embedding_cosine` - 文本向量索引（HNSW，余弦相似度）
- `idx_image_embedding_cosine` - 图像向量索引（HNSW，余弦相似度）

**存储函数**: `vector_db.py:285-351` - `upsert_opengraph_item()`

#### 2.2 临时存储：内存/前端
**位置**: 
- 前端内存（`background.js` 中的 `opengraphData`）
- API 响应中临时返回（不持久化）

**用途**:
- 当前 session 的临时数据
- 前端展示使用
- 刷新后丢失（除非已存储到数据库）

### 📝 检查要点

- [ ] **数据库名称**：检查 `.env` 中的 `ADBPG_DBNAME` 值（可能是 `postgres` 或 `knowledgebase`）
- [ ] **Schema 名称**：检查 `.env` 中的 `ADBPG_NAMESPACE` 值（默认 `cleantab`）
- [ ] **实际表路径**：通过日志确认，如 `Table cleantab.opengraph_items already exists`
- [ ] 数据库连接配置是否正确（`ADBPG_HOST`, `ADBPG_DBNAME`, `ADBPG_NAMESPACE`）？
- [ ] `upsert_opengraph_item()` 是否正确存储了 `text_embedding` 和 `image_embedding`？
- [ ] 索引是否创建成功（检查启动日志）？
- [ ] 数据是否持久化（重启后是否还在）？

**如何确认实际数据库**：
```bash
# 查看环境变量
echo $ADBPG_DBNAME  # 或检查 .env 文件

# 查看启动日志
# 应该看到类似：Schema 'cleantab' exists
# 实际表路径 = {ADBPG_DBNAME}.{ADBPG_NAMESPACE}.opengraph_items
```

---

## 3. Search 检索现在是怎么操作的？

### ✅ 搜索流程

#### 3.1 API 端点：`/api/v1/search`
**位置**: `main.py:304-400` - `search_content()`

**搜索策略**（优先级从高到低）:

**第一优先级：向量数据库搜索** ✅
```
如果配置了 ADBPG_HOST:
  1. 文本搜索：
     - 生成查询文本的 embedding（embed_text）
     - 调用 search_by_text_embedding(query_emb, top_k=20)
     - 使用 PostgreSQL 向量相似度搜索（<=> 操作符）
     - 返回相似度排序的结果
  
  2. 图像搜索：
     - 下载并处理查询图像
     - 生成查询图像的 embedding（embed_image）
     - 调用 search_by_image_embedding(query_emb, top_k=20)
     - 使用 PostgreSQL 向量相似度搜索
     - 返回相似度排序的结果
  
  如果数据库有结果 → 直接返回
  如果数据库无结果 → 降级到本地搜索
```

**第二优先级：本地内存搜索**（降级方案）
```
如果数据库无结果 或 未配置数据库:
  1. 使用传入的 opengraph_items（前端传入）
  2. 调用 search_relevant_items()
  3. 检查 items 是否有 embedding：
     - 有 → 使用向量相似度计算（sort_by_vector_similarity）
     - 无 → 使用模糊搜索（fuzzy_score）
  4. 返回排序后的结果
```

#### 3.2 向量数据库搜索函数
**位置**: `vector_db.py:393-491`

**文本搜索**: `search_by_text_embedding()`
```sql
SELECT url, title, description, image, site_name,
       tab_id, tab_title, text_embedding, image_embedding, metadata,
       1 - (text_embedding <=> $1::vector(1024)) AS similarity
FROM cleantab.opengraph_items
WHERE text_embedding IS NOT NULL
  AND (1 - (text_embedding <=> $1::vector(1024))) >= $2
ORDER BY text_embedding <=> $1::vector(1024)
LIMIT $3;
```

**图像搜索**: `search_by_image_embedding()`
```sql
SELECT url, title, description, image, site_name,
       tab_id, tab_title, text_embedding, image_embedding, metadata,
       1 - (image_embedding <=> $1::vector(1024)) AS similarity
FROM cleantab.opengraph_items
WHERE image_embedding IS NOT NULL
  AND (1 - (image_embedding <=> $1::vector(1024))) >= $2
ORDER BY image_embedding <=> $1::vector(1024)
LIMIT $3;
```

**说明**:
- `<=>` 是 PostgreSQL 的余弦距离操作符
- `1 - (embedding <=> query)` 转换为相似度（0-1，越大越相似）
- 使用 HNSW 索引加速搜索

#### 3.3 本地搜索函数
**位置**: `search/pipeline.py:169-257` - `search_relevant_items()`

**流程**:
```
1. 生成查询向量（query_vec）
   - 使用 embed_text(query_text) 生成 1024 维向量

2. 检查文档是否有 embedding
   - docs_with_embedding: 有 text_embedding 或 image_embedding
   - docs_without_embedding: 没有 embedding

3. 如果有 embedding：
   - 调用 sort_by_vector_similarity()
   - 计算文本相似度和图像相似度（两路融合）
   - 根据内容类型自适应权重：
     * 视觉站：0.4*text + 0.6*image
     * 文本站：0.8*text + 0.2*image
     * 默认：0.6*text + 0.4*image
   - 返回排序后的结果

4. 如果没有 embedding：
   - 使用模糊搜索（fuzzy_score）
   - 基于标题和描述的文本匹配
   - 返回排序后的结果
```

### 📝 检查要点

- [ ] 搜索是否优先使用向量数据库？
- [ ] 数据库搜索失败时是否正确降级到本地搜索？
- [ ] 相似度计算是否正确（使用余弦相似度）？
- [ ] 两路融合（文本+图像）是否正常工作？
- [ ] 索引是否被使用（检查查询计划）？

---

## 总结

### 数据流图

```
┌─────────────────────────────────────────────────────────────┐
│ 1. OpenGraph 抓取阶段                                        │
├─────────────────────────────────────────────────────────────┤
│ fetch_opengraph(url)                                         │
│   ↓                                                          │
│ _prefetch_embedding(result)                                  │
│   ↓                                                          │
│ 生成 text_embedding + image_embedding                        │
│   ↓                                                          │
│ upsert_opengraph_item() → 存储到数据库                       │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ 2. 存储位置                                                  │
├─────────────────────────────────────────────────────────────┤
│ 阿里云 AnalyticDB PostgreSQL                                 │
│   - 数据库: {ADBPG_DBNAME} (环境变量，可能是 postgres)        │
│   - Schema: {ADBPG_NAMESPACE} (默认 cleantab)                 │
│   - 表: opengraph_items                                       │
│   - 完整路径: {ADBPG_DBNAME}.{ADBPG_NAMESPACE}.opengraph_items│
│   - 字段: text_embedding vector(1024),                       │
│          image_embedding vector(1024)                        │
│   ⚠️ 实际值以运行时环境变量为准！                              │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ 3. 搜索阶段                                                  │
├─────────────────────────────────────────────────────────────┤
│ POST /api/v1/search                                          │
│   ↓                                                          │
│ [优先级1] 向量数据库搜索                                      │
│   - search_by_text_embedding()                               │
│   - search_by_image_embedding()                              │
│   - 使用 HNSW 索引加速                                        │
│   ↓                                                          │
│ [优先级2] 本地内存搜索（降级）                                │
│   - search_relevant_items()                                  │
│   - 两路相似度融合（文本+图像）                                │
│   - 或模糊搜索（无 embedding）                                │
└─────────────────────────────────────────────────────────────┘
```

### 关键检查项

- [x] ✅ Embedding 在 OpenGraph 抓取时自动生成并存储
- [x] ✅ 数据存储在阿里云 AnalyticDB PostgreSQL
- [x] ✅ 搜索优先使用向量数据库，失败时降级到本地搜索
- [x] ✅ 使用 HNSW 索引加速向量搜索
- [x] ✅ 支持文本和图像两路相似度融合

