# 桌宠状态机设计评估

## 📊 评估目标
以**增强用户体验**和**引导用户交互**为导向，设计桌宠的状态机系统。

## 🎯 核心设计原则

1. **引导性**：帮助用户发现和使用功能
2. **情感化**：通过状态变化传达情感，增强用户粘性
3. **反馈性**：及时响应用户操作和系统事件
4. **渐进式**：从简单到复杂，逐步引导用户

---

## 🔄 状态机设计（建议：8-10个核心状态）

### 一、基础状态（4个）

#### 1. **Idle（空闲/待机）**
- **触发条件**：初始状态、无交互时
- **表现**：轻微呼吸动画、偶尔眨眼、轻微摆动
- **引导作用**：吸引注意力，暗示可交互
- **持续时间**：30-60秒后自动转换到其他状态

#### 2. **Hover（悬停/关注）**
- **触发条件**：鼠标悬停在桌宠上
- **表现**：放大、发光、看向鼠标方向
- **引导作用**：明确提示"我可以交互"
- **持续时间**：持续到鼠标离开

#### 3. **Dragging（拖拽中）**
- **触发条件**：用户开始拖拽桌宠
- **表现**：跟随鼠标移动、轻微旋转、显示拖拽提示
- **引导作用**：确认拖拽操作，提供视觉反馈
- **持续时间**：整个拖拽过程

#### 4. **MenuOpen（菜单打开）**
- **触发条件**：点击桌宠，显示操作菜单
- **表现**：看向菜单方向、轻微点头、菜单按钮高亮
- **引导作用**：引导用户选择操作
- **持续时间**：菜单显示期间

---

### 二、任务执行状态（3个）

#### 5. **Cleaning（清理中）**
- **触发条件**：用户点击"清理"按钮
- **表现**：播放清理动画（泡泡效果）、旋转、兴奋表情
- **引导作用**：提供任务执行反馈，增强成就感
- **持续时间**：清理任务执行期间（2-5秒）

#### 6. **Success（成功/完成）**
- **触发条件**：清理任务成功完成
- **表现**：跳跃、旋转、显示成功表情、播放庆祝动画
- **引导作用**：强化正向反馈，鼓励继续使用
- **持续时间**：2-3秒后回到Idle

#### 7. **Waiting（等待反馈）**
- **触发条件**：任务执行中，等待系统响应
- **表现**：轻微摆动、看向屏幕、显示"思考"表情
- **引导作用**：告知用户系统正在处理
- **持续时间**：直到收到系统反馈

---

### 三、引导性状态（2-3个）

#### 8. **FirstTime（首次使用引导）**
- **触发条件**：用户首次看到桌宠
- **表现**：显示引导气泡、指向关键功能、播放引导动画
- **引导作用**：**核心引导状态**，帮助用户理解功能
- **持续时间**：完成引导流程后（30-60秒）

#### 9. **Hint（功能提示）**
- **触发条件**：用户长时间未使用某些功能、新功能上线
- **表现**：显示提示气泡、指向特定按钮、轻微跳动吸引注意
- **引导作用**：**主动引导**，帮助用户发现隐藏功能
- **持续时间**：用户交互后或超时（10-15秒）

#### 10. **Tired（疲惫/休息）**（可选）
- **触发条件**：长时间未交互、用户长时间未使用
- **表现**：打哈欠、闭眼、轻微缩小、显示"休息"状态
- **引导作用**：情感化表达，增加趣味性
- **持续时间**：用户交互后立即恢复

---

## 📈 状态转换图

```
[Idle] ←→ [Hover] ←→ [MenuOpen]
  ↓         ↓            ↓
[Dragging] → [Idle]
  ↓
[Cleaning] → [Waiting] → [Success] → [Idle]
  ↓
[FirstTime] → [Idle]
  ↓
[Hint] → [Idle]
  ↓
[Tired] ←→ [Idle]
```

---

## 🎨 状态优先级和冲突处理

### 优先级规则（从高到低）：
1. **Cleaning** > 所有其他状态（任务执行优先）
2. **FirstTime** > Idle/Hover（首次引导优先）
3. **Hint** > Idle（主动引导优先）
4. **Hover** > Idle（交互优先）
5. **Tired** < 所有交互状态（交互时取消休息）

### 状态冲突处理：
- 如果用户在**Cleaning**状态时点击，暂停清理动画，进入**MenuOpen**
- 如果**Hint**显示时用户开始交互，立即隐藏提示，进入对应交互状态

---

## 💡 引导交互设计建议

### 1. **首次使用流程**
```
FirstTime → 显示"拖拽图片到我这里收藏" → 
点击桌宠 → 显示"点击我查看功能" → 
打开菜单 → 高亮"清理"按钮 → 
完成首次清理 → Success → Idle
```

### 2. **功能发现引导**
```
Idle（30秒无交互）→ Hint → 
显示"试试拖拽图片收藏"或"点击我清理标签页" → 
用户交互 → 进入对应状态
```

### 3. **情感化反馈**
- **成功清理**：Success状态 + 显示清理数量 + 庆祝动画
- **长时间未使用**：Tired状态 + "我在这里等你哦~"
- **频繁使用**：Success后保持更久的兴奋状态

---

## 🔢 状态数量总结

### **核心建议：8-10个状态**

#### 最小化方案（6个）：
1. Idle（空闲）
2. Hover（悬停）
3. Dragging（拖拽）
4. MenuOpen（菜单）
5. Cleaning（清理中）
6. Success（成功）

#### 完整方案（10个）：
1. Idle（空闲）
2. Hover（悬停）
3. Dragging（拖拽）
4. MenuOpen（菜单）
5. Cleaning（清理中）
6. Waiting（等待）
7. Success（成功）
8. FirstTime（首次引导）
9. Hint（功能提示）
10. Tired（疲惫）

#### 扩展方案（12-15个，未来可扩展）：
- 增加：Error（错误）、Celebrating（庆祝）、Sleeping（睡眠）、Excited（兴奋）等

---

## 🎯 实现建议

### 阶段1：MVP（6个状态）
- 实现基础交互状态（Idle, Hover, Dragging, MenuOpen）
- 实现任务状态（Cleaning, Success）
- 完成基本引导

### 阶段2：增强（8-10个状态）
- 添加FirstTime引导状态
- 添加Hint提示状态
- 添加Waiting等待状态
- 优化状态转换动画

### 阶段3：情感化（10-12个状态）
- 添加Tired休息状态
- 添加更多情感表达
- 个性化状态表现

---

## 📝 技术实现要点

1. **状态管理**：使用状态机库（如XState）或自定义状态管理
2. **动画系统**：每个状态对应一组动画配置
3. **事件驱动**：用户操作和系统事件触发状态转换
4. **持久化**：保存用户引导完成状态，避免重复引导
5. **性能优化**：状态转换时使用CSS动画，避免重绘

---

## ✅ 结论

**推荐实现：8-10个核心状态**

- **必须状态（6个）**：Idle, Hover, Dragging, MenuOpen, Cleaning, Success
- **引导状态（2个）**：FirstTime, Hint
- **可选状态（2个）**：Waiting, Tired

这个数量既能满足用户体验引导的需求，又不会过度复杂化实现。可以根据开发资源分阶段实现。




